do set_size 300 300;

do scene "main" {
  ~Draw(gr) -> do
    # (new Pen(call Color::FromArgb(0, 0, 0, 254), 1))
	let c = call Color::FromArgb(127, 127, 127, 127) in
	let p = new Pen(c, 1) in
    gr.DrawLine (p, 10, 10, 100, 100)
  end;
  _ -> ();
};

do show_window "main";